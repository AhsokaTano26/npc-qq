# npc-qq

Vue + Vite 页面，适合部署到 Cloudflare Pages。

## 本地开发

```bash
npm install
npm run dev
```

## 部署到 Cloudflare Pages

1. 构建命令：`npm run build`
2. 输出目录：`dist`

## 修改内容

群组配置在 `src/content/site.js`，直接增删 `groups` 数组即可。
